# LLM/VLM 模型移植模板

> 说「按照 llm-porting skill 移植 [模型名]」启动，SKILL 会把本模板复制为 `<model>_memory.md`，请在副本上填写。
> 用户只填上半部分（基本信息 ~ 其他），下半部分「移植进度备忘」由 AI 在移植过程中自动维护，勿手动改。
> 本主模板保持空白，勿改。

---

## 基本信息

| 字段 | 填写 |
|------|------|
| 模型名称 | |
| HuggingFace ID 或本地路径 | |
| 模型介绍页 / 论文链接 | （可选） |

## 部署需求

| 字段 | 选择 |
|------|------|
| 支持的模态 | □ 纯文本  □ 文本+图片  □ 文本+图片+视频  □ 含音频 |
| 目标芯片（可多选） | □ BM1684X PCIe  □ BM1684X SoC  □ BM1688 SoC |
| 量化精度 | □ bf16（全精度）  □ w4bf16（4-bit 量化）  □ 其他: ____ |
| 最大序列长度 | （如 2048, 4096） |
| 最大图片分辨率 | （如 448×448, 952×952，不确定可留空由 AI 建议） |
| 编译模式 | □ 仅静态（固定 shape，编译快，推理快）  □ 静态+动态（先跑通静态，再补充动态）  □ 强制动态（仅动态 shape） |
| 历史记录支持 | □ 需要（先跑通不带历史的流程，再补充历史支持）  □ 不需要 |

## 环境信息

> ⚠️ 以下三个环境将贯穿整个适配流程，请务必准确填写。AI 会将环境信息持久化记忆，不会混淆。

### MLIR 编译环境（编译 bmodel 用）

| 字段 | 填写 |
|------|------|
| 环境类型 | □ Docker（镜像名: ____）  □ 源码编译  □ 远程服务器 |
| 访问方式 | （本地路径 / SSH 地址 / Docker 启动命令） |
| tpu-mlir 路径 | （仓库在本地的路径） |
| 激活方式 | `cd <tpu-mlir路径> && source ./envsetup.sh` |

### TPU 运行环境（运行 pipeline 用）

| 字段 | 填写 |
|------|------|
| 芯片型号 | □ BM1684X  □ BM1688 |
| 环境类型 | □ PCIE 卡  □ SoC（远程） |
| 访问方式 | （本地 / SSH 地址） |
| 激活方式 | （conda/venv 激活命令；无需激活则留空） |
| LLM-TPU 路径 | （AI 自动预填，若与实际运行路径不同请修改） |

### CUDA 环境（精度对比用，步骤 4 需要）

| 字段 | 填写 |
|------|------|
| 是否可用 | □ 有  □ 无（只能做端到端语义验证） |
| 访问方式 | （SSH 地址，AI 登录后自行查询 GPU 型号、Driver/CUDA 版本等） |
| 激活方式 | （conda/venv 激活命令；无需激活则留空） |
| 模型权重路径 | （HF 权重在 CUDA 机器上的路径） |

> ⚠️ 步骤 4 在此跑 HF 模型前，**先研究 torch / transformers 版本组合**（查模型卡 README + `nvidia-smi` driver），确认能跑该模型再装——torch 装错重来极费时。详见 verify §E。

## 其他

| 字段 | 填写 |
|------|------|
| 是否有时间或优先级约束 | （可选） |
| 参考的已有模型 | （如 "类似 Qwen3.5" 或 "参考 MiniCPM-V-4.6"，可选） |
| 备注 | （任何补充信息） |

---

## 移植进度备忘（AI 维护）

> 本节由 AI 在移植过程中持续更新，记录关键决策、命令和进度。
> 每步完成后更新，确保 context 压缩后仍能恢复关键信息。

### 关键决策

（AI 在步骤 1 分析完成后开始填写）

### 关键命令

（AI 在步骤 2 编译、步骤 3 运行成功后填写）

### 当前进度

- [ ] 步骤 1 架构分析
- [ ] 步骤 2 Converter 编译
- [ ] 步骤 3 Pipeline 跑通
- [ ] 步骤 4 精度验证
- [ ] 步骤 5 收尾提交
